/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable static export for potential static hosting
  // output: 'export',
  
  // Optimize images
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig



